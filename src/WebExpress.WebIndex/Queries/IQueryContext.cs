using System;

namespace WebExpress.WebIndex.Queries
{
    /// <summary>
    /// Represents a disposable context for executing queries.
    /// </summary>
    public interface IQueryContext : IDisposable
    {
    }
}
